## Why

`build-web-ui-poc` 完成後，將以此 change 作為 Web UI v1 正式版本的承載入口。v1 的具體範圍尚未拍板，待 PoC 跑過、收集使用回饋後再填入。

## What Changes

（待定）以下為 PoC 階段曾討論、預計於 v1 評估納入的候選功能，最終是否做、做到哪、優先序皆待確認：

- 自訂報表組合（時間區間、群組維度、欄位）
- CSV 匯出
- PDF 匯出（評估 Browsershot / Playwright 等方案）
- 報表狀態以 URL query 序列化以便分享
- email 排程寄送
- 命名報表（saved report）
- OAuth 登入（GitHub / Google）與白名單
- read-only DB user 切換

## Capabilities

### New Capabilities

（待定，於 PoC 後再列）

### Modified Capabilities

（待定，於 PoC 後再列）

## Impact

（待定）
